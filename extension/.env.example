# Vite build-time env for the Nemo Anki Chrome extension.
# Backend API origin (no trailing /api) — the extension's manifest host_permissions
# already covers both of the values below; if you point this somewhere else, add
# that origin to public/manifest.json's host_permissions too.
VITE_API_URL=http://localhost:8004
